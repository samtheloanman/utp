# Phase 3: Interactive Polling & Legislature UI

## Goal
Develop the "Legislature" UI showcasing the AI-generated debates and allowing users to cast votes.

## Tasks
- [ ] Build the dynamic issue pages (`/issue/[id]/page.tsx`) with real-time sentiment bars.
- [ ] Integrate Wagmi `useWriteContract` to call the `vote()` function on the RSK smart contract.
- [ ] Surface user voting data across the platform.
- [ ] Implement OpenGraph metadata for dynamic sharing of issue outcomes on social media (X/Meta).
