---
phase: 01-stablecoin-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - "src/app/layout.tsx"
  - "src/app/page.tsx"
  - "src/lib/wagmi.ts"
  - "src/components/ui/header.tsx"
  - "src/components/Providers.tsx"
autonomous: true
requirements:
  - "P1-STBL-3"

must_haves:
  truths:
    - "The Next.js application runs successfully."
    - "The application includes a basic layout with a header."
    - "Wagmi and Viem are installed and configured for RSK Testnet."
    - "A provider component wraps the application to provide Web3 context."
  artifacts:
    - path: "src/app/page.tsx"
      provides: "The main entry point for the application UI."
    - path: "src/lib/wagmi.ts"
      provides: "Wagmi configuration for connecting to RSK."
    - path: "src/components/Providers.tsx"
      provides: "The client-side component that wraps the app with Wagmi context."
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/Providers.tsx"
      via: "component composition"
      pattern: "<Providers>"
    - from: "src/components/Providers.tsx"
      to: "src/lib/wagmi.ts"
      via: "import and configuration"
      pattern: "createConfig"
---

<objective>
Set up the foundational Next.js application, including installing and configuring Web3 libraries (Wagmi, Viem), and creating a basic application layout. This plan prepares the frontend for future integration with the smart contracts.

Purpose: To create a runnable, Web3-ready frontend application structure.
Output: A Next.js application with Wagmi configured for RSK Testnet and a basic UI layout.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Frontend Dependencies</name>
  <files>
    - "package.json"
  </files>
  <action>
  1. Install necessary Web3 libraries: `wagmi`, `viem`, and `@tanstack/react-query`.
  2. The project context mentions Next.js 15 and React 19 are already in use, so we just need to add the Web3 specifics.
  </action>
  <verify>
    <automated>npm install wagmi viem @tanstack/react-query && grep "wagmi" package.json</automated>
  </verify>
  <done>
    `package.json` is updated with the new dependencies, and `npm install` completes successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Wagmi for RSK Testnet</name>
  <files>
    - "src/lib/wagmi.ts"
  </files>
  <action>
  1. Create a new file `src/lib/wagmi.ts`.
  2. Import `createConfig`, `http` from `wagmi`.
  3. Import the `rskTestnet` chain definition from `viem/chains`.
  4. Create and export a Wagmi config using `createConfig`, specifying `rskTestnet` as the chain and using a public HTTP transport.
  </action>
  <verify>
    <automated>The file `src/lib/wagmi.ts` should exist and contain a `createConfig` call.</automated>
  </verify>
  <done>
    A valid Wagmi configuration for RSK Testnet is exported from `src/lib/wagmi.ts`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Application Layout and Providers</name>
  <files>
    - "src/app/layout.tsx"
    - "src/app/page.tsx"
    - "src/components/Providers.tsx"
    - "src/components/ui/header.tsx"
  </files>
  <action>
  1. Create `src/components/Providers.tsx`. This will be a client-side component (`"use client"`) that wraps its children with `WagmiProvider` and `QueryClientProvider`.
  2. Create a basic `src/components/ui/header.tsx` component that displays the application title, "UTP Stablecoin Dashboard".
  3. Modify `src/app/layout.tsx` to import and use the `Providers` component, wrapping the main content of the application. Also include the `Header` component.
  4. Clean up the default `src/app/page.tsx` to display a simple welcome message like "Welcome to the UTP Dashboard."
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    The application builds successfully and has a root layout that includes the necessary Web3 providers and a basic header.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Browser -> Frontend | The user interacts with the application within their browser environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Spoofing | Frontend UI | Accept | At this stage, the risk is low as no critical actions are performed. The UI is a simple display. In later phases, this will be mitigated by ensuring all critical data is read directly from the blockchain and users verify transactions in their wallets. |
</threat_model>

<verification>
The phase is verified when the Next.js application starts without errors (`npm run dev`) and displays the main page with the header. The browser's developer console should show no errors related to Wagmi or providers.
</verification>

<success_criteria>
- The Next.js app is runnable.
- Wagmi is installed and configured for `rskTestnet`.
- A root layout exists with a header and Wagmi providers wrapping the content.
- `npm run build` completes without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stablecoin-foundation/01-02-SUMMARY.md`
</output>
